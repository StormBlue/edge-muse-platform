/** 自定义滚动条：Viewport + 纵向/横向 Scrollbar + Corner，与 Radix/Reka 惯例一致。 */
export { default as ScrollArea } from "./ScrollArea.vue";
/** 可单独复用；通常由 ScrollArea 内嵌，手动指定 `orientation`。 */
export { default as ScrollBar } from "./ScrollBar.vue";
