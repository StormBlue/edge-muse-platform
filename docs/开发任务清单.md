# Edge Muse Platform — 开发任务清单

> 与 [`开发需求.md`](./开发需求.md) 配套。版本: v1.0 · 起始日期: 2026-04-25
>
> **使用说明**:勾选完成的任务;每周五更新一次进度统计;任务有依赖时,先做被依赖的。
>
> 状态图例:`☐` 待办 · `🟡` 进行中 · `✅` 已完成 · `⏸` 阻塞 · `❌` 取消

---

## 0. 进度看板(总览)

更新此处的统计,无需深入下面每条。

```
Phase 0  Bootstrap                 [35/46]   ████░  76%
Phase 1  基础设施 & 认证                 [50/56]   ████░  89%
Phase 2  生图核心(MVP)                 [34/40]   ████░  85%
Phase 3  会话与历史                     [20/34]   ███░░  59%
Phase 4  管理员功能                     [10/17]   ███░░  59%
Phase 5  系统管理员功能                   [22/26]   ████░  85%
Phase 6  上线打磨                      [15/35]   ██░░░  43%
─────────────────────────────────────────────────────
总计:      186/254 项                   ████░  73%
```

里程碑(填实际日期):

| 里程碑                         | 计划   | 实际       |
| ------------------------------ | ------ | ---------- |
| M1 — 项目骨架可本地启动        | **\_** | 2026-04-26 |
| M2 — 用户登录可用              | **\_** | 2026-04-26 |
| M3 — 首张图可生成(MVP)         | **\_** | 2026-04-26 |
| M4 — 多模式 + 分页历史完成     | **\_** | 2026-04-26 |
| M5 — 管理员功能完成            | **\_** | **\_**     |
| M6 — 系统管理员 + 多服务商完成 | **\_** | **\_**     |
| M7 — 生产环境上线              | **\_** | **\_**     |

---

## Phase 0 — Bootstrap(项目脚手架与基线)

### 0.1 仓库与工具链

- ✅ **0.1.1** 初始化 Git 仓库,创建 `main` / `dev` 分支策略
- ✅ **0.1.2** 创建 monorepo 结构:`/server` `/web` `/docs`
- ✅ **0.1.3** 配置 `pnpm-workspace.yaml`(workspaces: server, web)
- ✅ **0.1.4** 根 `package.json` 加 scripts:`dev`、`build`、`lint`、`typecheck`、`test`
- ✅ **0.1.5** 配置 `.editorconfig`、`.gitignore`、`.nvmrc`(Node 20+)
- ✅ **0.1.6** 配置 ESLint v9 flat config(`eslint.config.ts`)+ Prettier
- ✅ **0.1.7** 配置 Husky + lint-staged(commit 钩子)
- ✅ **0.1.8** README.md 写项目简介 + 启动命令

### 0.2 Cloudflare 资源开通

> 依赖:Cloudflare 付费账号($5/月起)

- ⏸ **0.2.1** 升级到 Workers Paid
- ⏸ **0.2.2** 创建 D1 数据库(staging + production 各一个)
- ⏸ **0.2.3** 创建 R2 私有桶(staging + production 各一个,**不绑定公开域名**)
- ⏸ **0.2.4** 创建 KV namespace(staging + production)
- ⏸ **0.2.5** 创建 AI Gateway 实例,记录 endpoint URL
- ⏸ **0.2.6** 创建 Turnstile site(获取 site key + secret key)
- ⏸ **0.2.7** Cloudflare DNS:绑定计划用的子域名(`app.example.com`、`staging.example.com`)
- ⏸ **0.2.8** 准备 Cloudflare API Token(限定权限:Workers/D1/R2/KV/Workflows/DO 编辑)

### 0.3 服务端骨架(/server)

- ✅ **0.3.1** `pnpm create cloudflare@latest server -- --framework=hono`(或手工初始化)
- ✅ **0.3.2** 安装核心依赖:`hono`、`@hono/zod-validator`、`zod`、`drizzle-orm`、`@noble/hashes`(密码/HMAC)
- ✅ **0.3.3** 配置 `wrangler.jsonc`:绑定 D1/R2/KV/AI/DO/Workflows + 三环境(dev/staging/production)
- ✅ **0.3.4** 创建 `src/index.ts` 最小 Hono app(`/api/health` 返回 ok)
- ✅ **0.3.5** 配置 TypeScript(`tsconfig.json`)+ `cf-typegen`(`pnpm wrangler types`)
- ✅ **0.3.6** 本地启动 `pnpm wrangler dev`,验证 `/api/health`

### 0.4 前端骨架(/web)

- ✅ **0.4.1** `pnpm create vite@latest web -- --template vue-ts`
- ✅ **0.4.2** 安装核心依赖:`vue-router@4`、`pinia`、`pinia-plugin-persistedstate`、`@vueuse/core`、`vue-i18n@next`、`@intlify/unplugin-vue-i18n`
- ✅ **0.4.3** 安装 Tailwind v4:`pnpm add tailwindcss @tailwindcss/vite`
- ✅ **0.4.4** 配置 `vite.config.ts`(Tailwind 插件、proxy `/api` 到 wrangler)
- ✅ **0.4.5** 创建 `src/styles/globals.css`(`@import "tailwindcss"`)+ 导入到 `main.ts`
- ✅ **0.4.6** Shadcn-Vue 初始化:`pnpm dlx shadcn-vue@latest init`(选 Tailwind v4 + TS)
- ✅ **0.4.7** 设计 token:在 `globals.css` 写 `:root` 与 `.dark` 的 oklch 变量(参考需求 §8.2)
- ✅ **0.4.8** 创建 `App.vue` 最小布局 + `RouterView`
- ✅ **0.4.9** 配置 `router/index.ts`(占位路由)
- ✅ **0.4.10** 验证 `pnpm dev` 渲染、暗色切换、i18n 切换

### 0.5 整合(SPA + Worker 同部署)

- ✅ **0.5.1** 配置 Workers Static Assets:`assets.directory = "web/dist"` + `not_found_handling: single-page-application` + `run_worker_first: ["/api/*", "/ws/*"]`
- ✅ **0.5.2** 根 script:`pnpm build` 调用 `pnpm -F web build && pnpm -F server build`
- ✅ **0.5.3** 本地 `pnpm wrangler dev` 同时托管 SPA + API,验证刷新 SPA 路由不 404
- ⏸ **0.5.4** 部署到 staging:`pnpm wrangler deploy --env staging`,验证可访问

### 0.6 数据库迁移工具

- ✅ **0.6.1** 配置 `drizzle.config.ts`(指向 D1)
- ✅ **0.6.2** 写 placeholder schema(空表 + `_test`)
- ✅ **0.6.3** 跑 `pnpm drizzle-kit generate` 验证流程
- ⏸ **0.6.4** 跑 `pnpm wrangler d1 migrations apply <db> --local` / `--remote`
- ✅ **0.6.5** package.json scripts:`db:gen`、`db:migrate:local`、`db:migrate:staging`、`db:migrate:prod`

### 0.7 CI/CD

- ✅ **0.7.1** GitHub Actions `ci.yml`:lint + typecheck + test + build(PR 与 push)
- ✅ **0.7.2** GitHub Actions `deploy-staging.yml`:main 合并自动部署 staging
- ✅ **0.7.3** GitHub Actions `deploy-production.yml`:tag 触发(手动 approve)
- ⏸ **0.7.4** Actions Secrets:`CLOUDFLARE_API_TOKEN`、`CLOUDFLARE_ACCOUNT_ID`
- ✅ **0.7.5** README 加上"如何部署"章节

> **🏁 M1 — 项目骨架可本地启动 + 部署 staging 通过**

---

## Phase 1 — 基础设施 & 认证

### 1.1 数据库 Schema(完整版)

- ✅ **1.1.1** 写 `users` 表(uuid pk, email, password_hash, nickname, role, created_by, status, created_at, ...)
- ✅ **1.1.2** 写 `password_resets` 表(token, user_id, expires_at, used_at)
- ✅ **1.1.3** 写 `providers` 表
- ✅ **1.1.4** 写 `provider_keys` 表(encrypted_key blob)
- ✅ **1.1.5** 写 `user_provider_keys` 表
- ✅ **1.1.6** 写 `quotas` 表 + `quota_transactions` 表
- ✅ **1.1.7** 写 `sessions`、`messages` 表
- ✅ **1.1.8** 写 `tasks`、`image_objects` 表
- ✅ **1.1.9** 写 `audit_logs` 表
- ✅ **1.1.10** 加索引(见需求 §5.3)
- ✅ **1.1.11** drizzle 类型导出 `InferSelectModel<typeof users>` 等
- ⏸ **1.1.12** 跑迁移:本地 + staging
- ✅ **1.1.13** 写 seed 脚本(创建初始系统管理员账号 + 一个默认服务商示例)

### 1.2 加密、密码、JWT 工具

- ✅ **1.2.1** `lib/crypto.ts`:AES-GCM 加解密(用 Web Crypto)
- ✅ **1.2.2** `lib/password.ts`:argon2id(via `@noble/hashes/argon2`)hash + verify
- ✅ **1.2.3** `lib/jwt.ts`:HS256 签发 access(15min)+ refresh(7d),含 jti
- ✅ **1.2.4** `lib/turnstile.ts`:校验 Turnstile token
- ✅ **1.2.5** 单元测试覆盖 crypto / password / jwt

### 1.3 中间件

- ✅ **1.3.1** `middleware/auth.ts`:解析 cookie/Bearer → 校验 JWT → 注入 `c.var.user`
- ✅ **1.3.2** `middleware/role.ts`:`requireRole('sysadmin'|'admin'|'user')`
- ✅ **1.3.3** `middleware/csrf.ts`:对非 GET 请求校验 CSRF token
- ✅ **1.3.4** `middleware/logger.ts`:结构化日志输出
- ✅ **1.3.5** `middleware/error.ts`:统一错误响应格式
- ✅ **1.3.6** `middleware/rateLimit.ts`:基于 KV 的简单 sliding window

### 1.4 认证 API

- ✅ **1.4.1** `POST /api/auth/login`(email + password + Turnstile token → set httpOnly cookie + csrf cookie)
- ✅ **1.4.2** `POST /api/auth/logout`(清 cookie + 写 jti 黑名单到 KV)
- ✅ **1.4.3** `POST /api/auth/refresh`
- ✅ **1.4.4** `POST /api/auth/password/forgot`(生成 token,发邮件 — 集成 Resend / MailChannels)
- ✅ **1.4.5** `POST /api/auth/password/reset`(凭 token 改密码)
- ✅ **1.4.6** `POST /api/auth/password/change`(已登录,凭旧密改新密)
- ✅ **1.4.7** `GET /api/me`(当前用户 + 配额 + 角色)
- ✅ **1.4.8** `PATCH /api/me`(改昵称)
- ✅ **1.4.9** 请求体用 zod 校验
- ☐ **1.4.10** 认证 API 的 e2e 测试(Vitest + miniflare)

### 1.5 邮件发送

- ✅ **1.5.1** 选定 Resend(推荐,简单)或 MailChannels Worker 路径
- ✅ **1.5.2** `lib/mailer.ts`:统一接口 `sendMail(to, template, data)`
- ☐ **1.5.3** 模板:重置密码邮件(中英两版,根据用户 locale)
- ☐ **1.5.4** 配置 SPF/DKIM(若用 MailChannels)
- ⏸ **1.5.5** Staging 环境真实测发邮件

### 1.6 前端登录态

- ✅ **1.6.1** Pinia store `useAuthStore`:`user`, `quota`, `isAuthenticated`
- ✅ **1.6.2** `api/client.ts`:创建 hono RPC client + 自动携带 cookie/CSRF
- ✅ **1.6.3** Router guard:未登录跳 `/login`;已登录访问 `/login` 跳 `/workspace`
- ✅ **1.6.4** 角色守卫:`/admin/*` 要求 admin+,`/sysadmin/*` 要求 sysadmin
- ✅ **1.6.5** Axios/ofetch 拦截器:401 自动跳登录 + 刷新 refresh token

### 1.7 登录 / 注册 / 忘记密码 UI

- ✅ **1.7.1** `views/auth/Login.vue`:邮箱 + 密码 + Turnstile + i18n + 暗色
- ✅ **1.7.2** `views/auth/ForgotPassword.vue`
- ✅ **1.7.3** `views/auth/ResetPassword.vue`(读 query token)
- ✅ **1.7.4** 错误吐司(`sonner`)
- ✅ **1.7.5** 响应式(手机 + 桌面)
- ☐ **1.7.6** Playwright E2E:登录失败 → 成功流程

### 1.8 Layout 与导航

- ✅ **1.8.1** `AppHeader`:Logo + 主题切换 + 语言切换 + 用户菜单
- ✅ **1.8.2** `Sidebar`(可折叠,响应式)
- ✅ **1.8.3** 用户菜单:头像 + 昵称 + 配额徽章 + 设置 + 登出
- ✅ **1.8.4** `ThemeSwitch`:三态(auto/light/dark),持久化
- ✅ **1.8.5** `LangSwitch`:zh-CN / en-US,持久化
- ✅ **1.8.6** 404 / 403 页面

> **🏁 M2 — 用户登录可用**

---

## Phase 2 — 生图核心(MVP:文生图)

### 2.1 服务商抽象层

- ✅ **2.1.1** 定义 `ImageProvider` 接口(generate / supportedSizes / health)
- ✅ **2.1.2** `OpenAICompatibleProvider` 实现(覆盖 gpt-image-2 协议)
  - ✅ 文生图(Responses API + Legacy fallback)
  - ✅ 错误规整(把 4xx/5xx + body 转成 `ProviderError` with code)
  - ✅ 响应解析(支持 Responses API、Legacy Images、Chat Completions 三种结构)
  - ✅ 超时与重试策略
- ✅ **2.1.3** `providers/registry.ts`:按服务商 ID 取实例
- ⏸ **2.1.4** 集成 AI Gateway:把请求 URL 改为 `https://gateway.ai.cloudflare.com/v1/<acct>/<gw>/{provider}/...`(若 AI Gateway 支持透明代理)
- ✅ **2.1.5** 单元测试:mock 三种响应结构

### 2.2 配额扣减

- ✅ **2.2.1** `lib/quota.ts`:`tryConsume(userId, n)` — D1 事务原子扣减
- ✅ **2.2.2** 系统管理员豁免逻辑
- ✅ **2.2.3** 配额变更写 `quota_transactions`
- ☐ **2.2.4** 单元测试:并发场景、不足场景

### 2.3 图片对象存储与代理

- ✅ **2.3.1** `lib/r2.ts`:`putImage(key, bytes, mime)` / `getImage(key)`
- ✅ **2.3.2** R2 key 命名:`u/<userId>/y/<YYYY>/m/<MM>/<imageId>.png`
- ✅ **2.3.3** SHA256 计算 + 去重(同 sha 不重复存)
- ✅ **2.3.4** `GET /api/i/:imageId`:校验 JWT/Cookie + 权限 + R2.get + Cache API 写入
- ✅ **2.3.5** 短期 image token:登录态用户从 `/api/me` 拿到一个 `image_signing_key`(short-lived)?或直接走 cookie 模式(MVP 推荐 cookie)
- ✅ **2.3.6** 缓存策略:`Cache-Control: private, max-age=300` + Vary
- ☐ **2.3.7** 性能测试:连续访问命中 cache

### 2.4 Durable Object: TaskRoom

- ✅ **2.4.1** `do/TaskRoom.ts`:Hibernation 模式
- ✅ **2.4.2** 实现 `acceptWebSocket` + `webSocketMessage` + `webSocketClose`
- ✅ **2.4.3** RPC 方法:`updateStatus(payload)` 由 Workflow 调用 → broadcast 给所有 socket
- ✅ **2.4.4** 持久化最新状态到 `state.storage`,新连接立即推送最新值
- ✅ **2.4.5** Alarm:任务超时 10 分钟无更新 → 标记失败
- ☐ **2.4.6** 单元测试(用 miniflare DO simulator)

### 2.5 Workflow: GenerateImage

- ✅ **2.5.1** `workflows/GenerateImage.ts`(extends WorkflowEntrypoint)
- ✅ **2.5.2** Step 1 `init`:更新 task.status = running
- ✅ **2.5.3** Step 2 `callProvider`:调 ImageProvider.generate;失败重试 3 次指数退避
- ✅ **2.5.4** Step 3 `persistImages`:解码 base64 → R2.put → 写 image_objects
- ✅ **2.5.5** Step 4 `updateTask`:写 task 完成态、消息内容
- ✅ **2.5.6** Step 5 `notifyDO`:RPC 调用 TaskRoom.updateStatus
- ✅ **2.5.7** 错误处理:任意步骤最终失败 → 写失败态 + 通知 DO
- ☐ **2.5.8** 集成测试

### 2.6 生成 API

- ✅ **2.6.1** `POST /api/generate`:校验 + 配额预扣 + 创建 task 行 + 启动 Workflow + 返回 `{ taskId, wsUrl }`
- ✅ **2.6.2** `GET /api/tasks/:id`:返回最新状态(fallback for ws)
- ✅ **2.6.3** `POST /api/tasks/:id/cancel`(仅 queued 可取消)
- ✅ **2.6.4** `POST /api/tasks/:id/retry`(失败可重试,新任务,新配额)
- ✅ **2.6.5** WebSocket upgrade:`/ws/task/:id` → 路由到 DO
- ✅ **2.6.6** zod 校验 prompt 长度、size 合法性、n 合法性

### 2.7 前端:工作台核心(文生图最小可用)

- ✅ **2.7.1** `views/workspace/Workspace.vue` 整体布局(左栏 + 主区)
- ✅ **2.7.2** Pinia `useSessionStore`:当前会话、消息列表
- ✅ **2.7.3** `ChatInput` 组件:textarea + 发送按钮 + 参数按钮(size/n)
- ✅ **2.7.4** `ChatMessage` 组件:用户气泡 / 助手气泡(支持图片/loading/错误)
- ✅ **2.7.5** `ImageMessage`:1/2/3 列网格 + 单图大图打开
- ✅ **2.7.6** `composables/useTaskWebSocket.ts`:连接 WS + 自动重连 + 状态合并到 store
- ✅ **2.7.7** 提交流:input → POST /api/generate → 拿 taskId → 加占位消息 → WS 推到完成
- ✅ **2.7.8** 配额徽章实时更新(扣完防呆)
- ✅ **2.7.9** 失败 toast + 重试按钮
- ✅ **2.7.10** 大图查看模态(photoswipe 或自写):缩放 + 下载 + 复制 prompt
- ☐ **2.7.11** Playwright E2E:登录 → 输入 prompt → 看到生成 → 大图 → 下载

> **🏁 M3 — 首张图可生成(MVP):用户登录后,文生图 → 出图 → 大图查看**

---

## Phase 3 — 会话与历史 + 图生图 + 对话

### 3.1 会话管理

- ✅ **3.1.1** `POST /api/sessions`:创建会话(模式 + 默认参数)
- ✅ **3.1.2** `GET /api/sessions`:列表(分页 + 搜索 + 排序)
- ✅ **3.1.3** `GET /api/sessions/:id`:详情
- ✅ **3.1.4** `PATCH /api/sessions/:id`:改标题、改默认参数、归档
- ✅ **3.1.5** `DELETE /api/sessions/:id`:软删除
- ✅ **3.1.6** 自动生成会话标题(首条 prompt 取前 20 字)
- ✅ **3.1.7** 前端:会话列表组件 + 新会话按钮
- ✅ **3.1.8** 切换会话 URL:`/workspace/s/:sessionId`

### 3.2 消息分页

- ✅ **3.2.1** `GET /api/sessions/:id/messages?cursor=&limit=` (倒序游标分页)
- ✅ **3.2.2** 前端:`composables/usePaginatedMessages.ts`(无限向上加载)
- ✅ **3.2.3** Intersection Observer 触发加载
- ✅ **3.2.4** 跨会话切换不跨数据状态
- ✅ **3.2.5** 性能:超长会话(>500 条)流畅

### 3.3 图生图(image-to-image)

- ✅ **3.3.1** `POST /api/uploads`:多文件 multipart,服务端校验 MIME + 大小
- ✅ **3.3.2** R2 上传 + 返回 imageId 列表
- ✅ **3.3.3** `OpenAICompatibleProvider.generate` 增加图生图分支(payload 含 input_image)
- ✅ **3.3.4** Workflow 适配:支持参考图作为输入
- ✅ **3.3.5** 前端 `ImageUploader` 组件:拖入 + 粘贴 + 点击 + 多图缩略图网格
- ✅ **3.3.6** 客户端图片压缩(>1.5MB / >2048边 → 压到 ≤2048 + JPEG q=0.85)
- ✅ **3.3.7** 模式切换 UI:文生图 / 图生图 / 对话
- ☐ **3.3.8** Playwright E2E:上传 → 图生图 → 输出

### 3.4 蒙版编辑(可选,MVP 后)

- ☐ **3.4.1** `MaskEditor` 组件(Konva.js):笔刷 / 擦除 / 笔刷大小
- ☐ **3.4.2** 生成"原图 + 半透明红色蒙版叠加图",作为第二张参考图随请求一起发
- ☐ **3.4.3** Provider 层 prompt 模板自动注入"仅修改红色区域"指令
- ☐ **3.4.4** 触屏支持(平板)

### 3.5 连续对话

- ✅ **3.5.1** Workflow 增加 chat 分支:把 session 的历史 messages 拼为 OpenAI messages 数组
- ✅ **3.5.2** 上下文裁剪策略:超过 N 条只保留最近 K 条 + 系统消息
- ✅ **3.5.3** 助手消息可能含图(image_generation_call)也可能纯文本
- ✅ **3.5.4** 前端:对话气泡区分文本/图片
- ✅ **3.5.5** 测试:多轮对话保持上下文

### 3.6 历史页 `/history`

- ✅ **3.6.1** `views/history/History.vue`:会话卡片网格(缩略图 + 标题 + 时间 + 计数)
- ✅ **3.6.2** 搜索栏(标题 / prompt 模糊匹配)
- ✅ **3.6.3** 排序:最近 / 最早 / 任务最多
- ✅ **3.6.4** 点击进入会话页

### 3.7 大图查看增强

- ✅ **3.7.1** 同会话图片间 ←/→ 切换
- ✅ **3.7.2** 显示 size + bytes + 任务 id
- ✅ **3.7.3** 复制 prompt
- ✅ **3.7.4** 删除消息(D1 软删 + R2 后台清理)

> **🏁 M4 — 多模式 + 分页历史完成**

---

## Phase 4 — 管理员功能

### 4.1 管理员 API

- ✅ **4.1.1** `GET /api/admin/users`(分页、过滤 q + status)
- ✅ **4.1.2** `POST /api/admin/users`(创建普通用户:邮箱、密码、初始配额)
- ✅ **4.1.3** `PATCH /api/admin/users/:id`(改昵称、状态)
- ✅ **4.1.4** `GET /api/admin/users/:id/quota`(配额详情 + 流水分页)
- ✅ **4.1.5** `POST /api/admin/users/:id/quota`(加/减配额,扣 admin 自身余额)
- ✅ **4.1.6** `GET /api/admin/users/:id/usage`(用量统计:成功率、模式占比、近 30 日趋势)
- ✅ **4.1.7** 权限校验:admin 只能操作自己创建的用户;sysadmin 可操作所有
- ✅ **4.1.8** 自动继承密钥:admin 创建 user 时,user 默认绑定 admin 的 provider_key
- ☐ **4.1.9** 单元测试:权限边界、配额扣减原子性

### 4.2 管理员 UI

- ✅ **4.2.1** `views/admin/UserList.vue`:表格 + 分页 + 搜索 + 状态筛选
- ✅ **4.2.2** "创建用户" 模态框
- 🟡 **4.2.3** 用户详情抽屉:基本信息、配额条、用量图(柱状/折线)
- 🟡 **4.2.4** 改配额对话框(显示自身剩余 + 不可超额)
- ☐ **4.2.5** 禁用 / 启用 / 重发邀请邮件按钮
- ☐ **4.2.6** 配额流水分页表格
- ☐ **4.2.7** Playwright E2E:admin 创建普通用户 → 普通用户登录可用

### 4.3 任务统计基础组件

- ☐ **4.3.1** 引入图表库(`echarts` 或 `unovis-vue` 或 `vue-chart-3`)
- ☐ **4.3.2** `StatLineChart` `StatBarChart` `StatPieChart` `StatKPICard`
- ☐ **4.3.3** 暗色模式适配

> **🏁 M5 — 管理员功能完成**

---

## Phase 5 — 系统管理员功能 + 多服务商

### 5.1 服务商管理

- ✅ **5.1.1** `GET/POST/PATCH/DELETE /api/sysadmin/providers`
- ✅ **5.1.2** 表单:name、base_url、default_model、supported_sizes(JSON)、enabled
- ✅ **5.1.3** "测试连通性" 按钮 → `POST /api/sysadmin/providers/:id/test`(用最小 prompt 检验)
- ✅ **5.1.4** 删除前检查无关联密钥
- ✅ **5.1.5** UI:`views/sysadmin/Providers.vue` 列表 + 详情 + 创建/编辑

### 5.2 密钥管理

- ✅ **5.2.1** `GET/POST/PATCH/DELETE /api/sysadmin/provider-keys`
- ✅ **5.2.2** 创建时本地 AES-GCM 加密 → 仅显示末 4 位
- ✅ **5.2.3** 不允许编辑 key 本身,只允许标记 disable + 重新创建
- ✅ **5.2.4** 测试密钥:`POST /api/sysadmin/provider-keys/:id/test`
- ✅ **5.2.5** UI:`views/sysadmin/Keys.vue`(列表 + 创建表单)
- ✅ **5.2.6** 显示:label + 末4位 + 已分配/已用配额 + 归属 admin

### 5.3 创建管理员 + 分配密钥

- ✅ **5.3.1** `POST /api/sysadmin/admins`(role=admin,选 provider_key + 总配额)
- ✅ **5.3.2** UI:`views/sysadmin/Admins.vue`(列表 + 创建模态)
- ☐ **5.3.3** 修改某 admin 的密钥(转移其下属用户的密钥关系)
- 🟡 **5.3.4** 修改 admin 总配额
- ☐ **5.3.5** Playwright E2E:sysadmin 创建 admin → admin 创建 user → user 生图,密钥归属正确

### 5.4 系统管理员看会话内容

- ✅ **5.4.1** `GET /api/sysadmin/users/:id/sessions`(任意用户的会话)
- ✅ **5.4.2** `GET /api/sysadmin/sessions/:id/messages`
- ✅ **5.4.3** 图片访问:Worker 代理已对 sysadmin 开放(权限校验通过)
- ☐ **5.4.4** UI:`views/sysadmin/UserSessions.vue` 树视图

### 5.5 全局看板

- ✅ **5.5.1** `GET /api/sysadmin/dashboard/stats`(KPI:用户数/任务数/成功率/Top错误码/Top服务商)
- ✅ **5.5.2** 7/30 天任务数趋势
- 🟡 **5.5.3** 服务商占比饼图
- 🟡 **5.5.4** Top 用户用量表
- ✅ **5.5.5** UI:`views/sysadmin/Dashboard.vue`
- ✅ **5.5.6** 缓存:看板数据 KV 缓存 60s

### 5.6 系统管理员个人偏好

- ✅ **5.6.1** `PATCH /api/sysadmin/preferences`(选择默认 provider + key)
- ☐ **5.6.2** UI:`views/sysadmin/Preferences.vue`
- ✅ **5.6.3** 自身生图调用走自己选的 key

> **🏁 M6 — 系统管理员 + 多服务商完成**

---

## Phase 6 — 上线打磨

### 6.1 安全加固

- ✅ **6.1.1** Turnstile:登录 / 注册 / 忘记密码必接(已在 Phase 1 实现,这里复核)
- ✅ **6.1.2** 速率限制:登录 5次/15min,生图 60次/min
- ✅ **6.1.3** XSS:所有用户内容 v-html 清洗或不用 v-html
- ✅ **6.1.4** CSRF:cookie 模式下检查 X-CSRF-Token
- ✅ **6.1.5** CORS:严格 same-origin(SPA + API 同 worker → 默认 OK)
- ✅ **6.1.6** 头部安全:HSTS、X-Frame-Options、Content-Security-Policy
- 🟡 **6.1.7** 审计日志覆盖完整(写操作全入 audit_logs)
- ☐ **6.1.8** 安全审查:`/security-review` 命令跑过

### 6.2 可观测性

- ✅ **6.2.1** Workers Logs 结构化日志(traceId 贯穿)
- ☐ **6.2.2** Workflow 失败告警:每天扫一次失败任务数,超阈值发邮件
- ☐ **6.2.3** AI Gateway 看板检视(成本、错误率、cache-hit)
- ☐ **6.2.4** 慢任务监控:>120s 任务记日志
- ☐ **6.2.5** D1 size 监控

### 6.3 性能与体验

- ✅ **6.3.1** 前端代码分包:`router` 懒加载、`echarts` 按需
- ✅ **6.3.2** 图片懒加载(IntersectionObserver)
- ☐ **6.3.3** 列表虚拟化(超长会话用 `vue-virtual-scroller` 或自实现)
- ☐ **6.3.4** Lighthouse:Performance > 85,Accessibility > 90
- ✅ **6.3.5** 首屏 JS gzip < 200KB
- ✅ **6.3.6** WebSocket 在弱网下的重连体验
- ☐ **6.3.7** 触屏 / 键盘 全可用

### 6.4 数据治理

- ✅ **6.4.1** 删除消息 → 30 天后清理 R2 对象的 cron worker
- ☐ **6.4.2** 备份策略:每天 export D1 → R2(via cron worker)
- ☐ **6.4.3** Time Travel 演练:回滚到 30min 前

### 6.5 文档

- ✅ **6.5.1** README:本地开发 / 环境变量 / 部署步骤
- ✅ **6.5.2** ARCHITECTURE.md:简短架构图
- ✅ **6.5.3** OPERATIONS.md:常见运维场景(数据库迁移、密钥轮换、回滚)
- ✅ **6.5.4** CHANGELOG.md
- ✅ **6.5.5** API 文档(基于 hono/zod 自动生成 OpenAPI 或手写)

### 6.6 上线

- ⏸ **6.6.1** 测试 production 环境部署
- ⏸ **6.6.2** DNS 切换到生产域名
- ⏸ **6.6.3** 创建初始系统管理员账号(seed 或人工)
- ⏸ **6.6.4** 烟雾测试清单:登录 → 生图 → 大图 → 历史 → 管理员 → 系统管理员
- ⏸ **6.6.5** 回滚预案验证

### 6.7 验收

- ☐ **6.7.1** 与原始基础需求对照逐项验证(见需求文档附录 A)
- ☐ **6.7.2** 用户文档(对最终用户)
- ☐ **6.7.3** 内部演示

> **🏁 M7 — 生产环境上线**

---

## 本轮开发回顾

- 2026-04-26: 已完成本地 monorepo、Worker + Vue 骨架、认证、D1 schema、mock provider、生图任务、R2 私有图片代理、基础管理后台和系统管理后台。
- 2026-04-26: 完成 Shadcn-Vue 初始化配置修正。CLI 生成的 `reka-nova` 注册项不可访问,已改为当前可访问的 `new-york` 样式配置,并补齐 `@/lib/utils`。自查: `pnpm -F web typecheck`、`pnpm -F web lint` 通过。
- 2026-04-26: 完成 Drizzle 生成流程验证。使用临时输出目录执行 `pnpm -F server exec drizzle-kit generate --schema ./src/db/schema.ts --dialect sqlite --driver d1-http --out /tmp/edge-muse-drizzle-verify --name verify_schema`,成功生成 12 张表 SQL；未写入正式 migrations,避免与手写初始迁移重复。
- 2026-04-26: 完成 M3 回顾修正。修复 Vue toast 包误用 React `sonner` 导致的运行时 hook 错误；修复 `/ws/task/:id` 与 `/api/ws/task/:id` 挂载不一致导致实时推送 404；TaskRoom 失败事件补齐 taskId，避免前端多任务状态合并错误；大图查看补齐缩放、下载、复制 prompt、同会话切换与删除消息入口。自查: `pnpm typecheck`、`pnpm test`、`pnpm lint` 通过；Playwright CLI 已验证登录与提交生图流程，后续继续补完整下载断言。
- 2026-04-26: 完成 M4 第一轮回顾修正。补齐消息向上分页触发与滚动位置保持、参考图拖拽/粘贴/点击上传和客户端压缩、chat 历史上下文裁剪与纯文本回复落库、历史最近/最早/任务最多排序、消息删除时同步软删图片元数据；WebSocket 手动关闭不再误触发无限重连。补充 provider chat 纯文本测试。自查: `pnpm typecheck`、`pnpm lint`、`pnpm test` 通过。
- 已通过: `pnpm typecheck`、`pnpm test`、`pnpm lint`、`pnpm build`。
- 已做本地烟雾测试: `/api/health`、SPA 路由 `/workspace`、登录 `sysadmin@example.com/password123`、`POST /api/generate`、任务查询、消息分页、`/api/i/:imageId` 私有图片读取。
- 阻塞项集中在 Cloudflare 真实资源开通、Actions Secrets、staging/production 远程迁移与部署、真实邮件/DNS/Turnstile 配置。

---

## 附录 A:跨阶段技术债 / 持续任务

这些不属于某个 Phase,持续运营:

- ☐ **A.1** 依赖周更:Vue/Tailwind/Hono/Drizzle 主版本升级
- ☐ **A.2** Workers `compatibility_date` 季度推进
- ☐ **A.3** Cloudflare 新功能跟进:Containers GA、Secrets Store GA、其他
- ☐ **A.4** AI Gateway 配置定期复核(限流阈值、缓存)
- ☐ **A.5** 错误日志周扫查
- ☐ **A.6** 性能基准:每月跑一次 Lighthouse + 真实生图链路计时

---

## 附录 B:依赖关系速查图

```
Phase 0 (Bootstrap)
   │
   ├─► Phase 1 (基础设施 & 认证)
   │      │
   │      └─► Phase 2 (生图核心 MVP)
   │             │
   │             └─► Phase 3 (会话/历史/图生图/对话)
   │                    │
   │                    └─► Phase 4 (管理员)
   │                           │
   │                           └─► Phase 5 (系统管理员 + 多服务商)
   │                                  │
   │                                  └─► Phase 6 (上线打磨)
   │
   └─► (Phase 0.7 CI/CD 与 Phase 0.6 数据库迁移工具贯穿全程)
```

关键阻塞点提醒:

- **Phase 2.4 (DO) ↔ Phase 2.5 (Workflow)**:必须先实现 DO 的 RPC 接口,Workflow 才能调用通知。
- **Phase 5.3 (创建 admin) ↔ Phase 5.2 (密钥)**:密钥要先存在才能在创建 admin 时引用。
- **Phase 1.5 (邮件) ↔ Phase 1.4 (忘记密码)**:邮件可以晚于忘记密码 API,先打通核心登录流程。

---

## 附录 C:任务模板(添加新任务时复制)

```markdown
- ☐ **X.Y.Z** 任务标题
  - 验收标准:
  - 依赖:
  - 预估工时:
  - 负责人:
```

---

## 附录 D:工时估算(粗,实际以执行情况调整)

| Phase               |  估算(人日) | 备注                    |
| ------------------- | ----------: | ----------------------- |
| Phase 0 Bootstrap   |           5 | 含 CF 资源开通          |
| Phase 1 认证        |           8 | 含邮件、Turnstile       |
| Phase 2 生图核心    |          12 | 含 DO + Workflow,最大块 |
| Phase 3 会话/多模式 |          10 | 含图生图、对话、分页    |
| Phase 4 管理员      |           5 |                         |
| Phase 5 系统管理员  |           7 |                         |
| Phase 6 打磨        |           6 |                         |
| **总计**            | **53 人日** | 约 2.5 个月单人         |

并发开发建议:

- 一人主推服务端;一人主推前端(Phase 0 之后可并行)
- Phase 4/5 可前端先做静态页 + 后端晚 1 周跟上

---

> **下一步行动**:从 Phase 0 第一项开始勾选。每完成一个 Phase 后,在"进度看板"更新数字与里程碑日期。
